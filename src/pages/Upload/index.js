import React from 'react';
import Header from '~/components/Layout/DefaultLayout/Header';

const Upload = () => {
    return (
        <div>
            <Header />\<div>Upload</div>
        </div>
    );
};

export default Upload;
